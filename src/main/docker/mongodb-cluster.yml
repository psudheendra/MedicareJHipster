version: '2'
services:
    medicare-mongodb:
        image: mongo:3.6.3
        ports:
            - "27017:27017"
        command: mongos --configdb medicare-mongodb-config
    medicare-mongodb-node:
        build:
            context: .
            dockerfile: mongodb/MongoDB.Dockerfile
        command: mongod --replSet rs1 --noprealloc --smallfiles
    medicare-mongodb-config:
        image: mongo:3.6.3
        container_name: medicare-mongodb-config
        command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db
